# Wi-Fi Analyzer 操作説明書 (User Manual)

周囲の Wi-Fi 電波状況をリアルタイムにスキャン・可視化するデスクトップアプリケーション「**Wi-Fi Analyzer**」の操作説明書です。

---

## 目次
1. [画面構成と各部の名称](#1-画面構成と各部の名称)
2. [基本的な使い方](#2-基本的な使い方)
3. [各機能の詳細操作](#3-各機能の詳細操作)
   - [スキャンの制御（自動 / 手動 / 間隔変更）](#スキャンの制御自動--手動--間隔変更)
   - [周波数帯（バンド）フィルター](#周波数帯バンドフィルター)
   - [インクリメンタル検索（SSID / BSSID）](#インクリメンタル検索ssid--bssid)
   - [お気に入り（ピン留め）とお気に入り上位表示](#お気に入りピン留めとお気に入り上位表示)
   - [SSID の並び替え（ソート）](#ssid-の並び替えソート)
   - [表示言語の切り替え](#表示言語の切り替え)
4. [メッシュ Wi-Fi（EasyMesh 等）の電波確認](#4-メッシュ-wifi-easymesh-等の電波確認)
5. [設定とウィンドウ状態の自動保存](#5-設定とウィンドウ状態の自動保存)
6. [インストール・アンインストール方法](#6-インストールアンインストール方法)
7. [よくある質問・トラブルシューティング (FAQ)](#7-よくある質問トラブルシューティング-faq)

---

## 1. 画面構成と各部の名称

ウィンドウは大きく分けて 4 つのエリアで構成されています。

```
┌─────────────────────────────────────────────────────────────┐
│ ① ツールバー（スキャン制御・フィルター・検索・言語）        │
├─────────────────────────────────────────────────────────────┤
│ ② 接続中アクセスポイント情報カード                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ③ アクセスポイント一覧（メイングリッド）                    │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ ④ ステータスバー（スキャン状況・検出件数・警告案内）        │
└─────────────────────────────────────────────────────────────┘
```

### ① ツールバー（最上部）
- **アプリタイトル & アイコン**
- **スキャン制御**:
  - **自動スキャン トグルボタン**: 自動スキャン（ON / OFF）を切り替えます。
  - **更新ボタン (🔄)**: 今すぐスキャンを実行します。
  - **間隔ドロップダウン**: 自動スキャンの更新間隔（3秒 / 5秒 / 10秒）を選択します。
- **バンドフィルター**: 「すべて」「2.4 GHz」「5 GHz」「6 GHz」ボタンで周波数帯を素早く切り替えます。
- **検索ボックス**: SSID（ネットワーク名）または BSSID（MAC アドレス）をリアルタイムに絞り込みます。
- **言語切り替え**: 「Auto」「日本語」「English」から UI 表示言語を選択します。

### ② 接続中アクセスポイント情報カード
現在お使いの PC が接続している Wi-Fi アクセスポイントの詳細情報が表示されます。
- **状態バッジ**: 接続中（青色）
- **SSID**: 接続中のネットワーク名
- **BSSID**: 接続している AP の物理アドレス（MAC アドレス）
- **バンド・チャンネル・周波数**: 接続中の無線規格と周波数（例: `5 GHz`, `36 (5180 MHz)`）
- **セキュリティ**: 認証および暗号化方式（例: `WPA2-Personal / CCMP`）
- **電波強度**: グラフバーおよびパーセンテージ・RSSI（例: `82% (-59 dBm)`）
- **使用アダプター**: 通信に使用している Wi-Fi カードの名称

### ③ アクセスポイント一覧（メイングリッド）
スキャンされたすべてのアクセスポイントが一覧表示されます。

| 列名 | 内容 | 説明 |
| :--- | :--- | :--- |
| **📌 (ピン留め)** | ☆ / ★ | クリックでお気に入り登録/解除します。 |
| **状態** | `接続中` | 現在接続している AP のみに青色バッジが表示されます。 |
| **SSID** | ネットワーク名 | 非公開時は *`<非公開ネットワーク>`* と斜体表示されます。 |
| **BSSID** | MAC アドレス | 各 AP 固有の識別子（例: `98:2D:BA:35:38:52`）です。 |
| **電波強度** | カラーバー & 数値 | 緑（良好）、橙（普通）、赤（微弱）のバーと `品質% (RSSI dBm)`。 |
| **周波数帯** | バッジ | `2.4 GHz`（橙）、`5 GHz`（青）、`6 GHz`（紫）で識別。 |
| **チャンネル** | 数値 | AP が利用している無線チャンネル番号。 |
| **周波数** | MHz 表示 | 中心周波数（例: `5220 MHz`）。 |
| **セキュリティ** | 文字列 | 認証方式と暗号化方式（例: `WPA2-Personal / CCMP`）。 |
| **規格** | 文字列 | 802.11 規格（例: `He` = Wi-Fi 6, `Vht` = Wi-Fi 5, `Ht` = Wi-Fi 4）。 |

### ④ ステータスバー（最下部）
- **左側**: スキャン進行インジケーター、検出されたアクセスポイント総数、最終スキャン完了時刻が表示されます。
- **右側**: Wi-Fi アダプターが無効な場合や、Windows の位置情報サービス設定による警告メッセージが表示されます。

---

## 2. 基本的な使い方

1. **アプリを起動する**:
   - 起動と同時に自動スキャンが開始され、周囲の電波一覧が即座に表示されます。
   - すでに Wi-Fi に接続している場合は、上部に接続中カードが表示されます。
2. **電波状況を確認する**:
   - 電波強度のカラーバー（緑：強力、橙：中程度、赤：微弱）や RSSI 値（-dBm）で、どの AP の電波が強いかを確認します。
3. **終了する**:
   - ウィンドウを閉じると、現在のウィンドウ位置・サイズ、お気に入り、フィルター設定などが自動保存され、次回起動時に復元されます。

---

## 3. 各機能の詳細操作

### スキャンの制御（自動 / 手動 / 間隔変更）
- **自動スキャンを停止 / 再開する**:
  - ツールバーの「自動スキャン」スイッチをクリックして OFF にすると、定期スキャンが一時停止します。再度クリックすると再開します。
- **今すぐスキャンを実行する**:
  - 「🔄」ボタンをクリックすると、自動スキャンの設定にかかわらず即座に最新の電波状況を取得します。
- **スキャン間隔の変更**:
  - 「3秒」「5秒」「10秒」から選択できます。電波の移動や切り替わりを素早く追従したいときは「3秒」、PC のバッテリー消費を抑えたいときは「10秒」が適しています。

### 周波数帯（バンド）フィルター
ツールバーのボタンをクリックすることで、指定した周波数帯のみに絞り込めます。
- **すべて**: 周囲の全アクセスポイントを表示します。
- **2.4 GHz**: 壁などの遮蔽物に強い 2.4 GHz 帯のみを表示します。
- **5 GHz**: 高速通信が可能な 5 GHz 帯（W52 / W53 / W56）のみを表示します。
- **6 GHz**: Wi-Fi 6E / Wi-Fi 7 で利用される干渉の少ない 6 GHz 帯のみを表示します。

### インクリメンタル検索（SSID / BSSID）
ツールバー右側の検索ボックスにキーワードを入力すると、入力中にリアルタイムで該当するアクセスポイントのみが絞り込まれます。
- **SSID 検索**: 自宅やオフィスのルーター名などを入力して絞り込み。
- **BSSID 検索**: MAC アドレス（例: `98:2D` や `35:38` など）の一部を入力して特定ハードウェアを追跡。

### お気に入り（ピン留め）とお気に入り上位表示
特定のアクセスポイントをマークして素早く見つけることができます。
- **ピン留めの登録 / 解除**:
  - 一覧の一番左列にある「☆」をクリックすると「★」（黄色）に変わり、お気に入りに登録されます。もう一度クリックすると解除されます。
- **お気に入りを上位表示 (📌)**:
  - リスト上部または SSID 列ヘッダーの「📌 お気に入りを上位表示」を ON にすると、お気に入りに登録した AP が電波強度にかかわらず**一覧の最上部に固定表示**されます。

### SSID の並び替え（ソート）
一覧の [SSID] 列ヘッダーをクリックすると、並び順を切り替えられます。
1. **既定（電波強度順）**:
   - 接続中 AP → 電波強度の強い順に並びます。
2. **▲ A-Z（昇順）**:
   - SSID 名のアルファベット順・五十音順（昇順）に並び替えます。同一 SSID 内では電波強度の強い順になります。
3. **▼ Z-A（降順）**:
   - SSID 名の降順に並び替えます。
- 列ヘッダー右側の「▾」ボタンから、直接目的の並び順を選択することも可能です。

### 表示言語の切り替え
ツールバー右上のドロップダウンから表示言語をいつでも変更できます。
- **Auto**: Windows の表示言語モードに自動追従します（日本語 Windows では日本語、それ以外では英語）。
- **日本語**: 強制的に日本語表示にします。
- **English**: 強制的に英語表示にします。

---

## 4. メッシュ Wi-Fi（EasyMesh 等）の電波確認

本アプリは、メッシュ Wi-Fi や同一 SSID で複数 AP を配置している環境（EasyMesh、サテライト、オフィス AP）の調査に最適です。

- **子機（Agent）と親機（Controller）の個別表示**:
  - SSID が同一であっても、ハードウェア固有の **BSSID（MAC アドレス）ごとに別々の行** として表示されます。
  - 各行で異なる「チャンネル」「周波数帯」「電波強度」を個別に比較できます。
- **ローミングの確認**:
  - PC を部屋間で移動した際、現在どの AP（親機か子機か）に接続しているかが上部の「接続中カード」および行の「接続中バッジ」でリアルタイムに確認できます。
- **お気に入りの活用**:
  - 自室に近い子機の BSSID だけをピン留め（★）しておくことで、移動時の電波減衰や切り替わりを容易に監視できます。

---

## 5. 設定とウィンドウ状態の自動保存

以下の設定はアプリ終了時に自動的に保存され、次回起動時に復元されます。
- ウィンドウの表示位置、幅・高さ、最大化状態
- 自動スキャンの ON / OFF 状態
- スキャン間隔（3秒 / 5秒 / 10秒）
- 選択中の周波数帯フィルター
- SSID のソート順（既定 / 昇順 / 降順）
- お気に入り上位表示の ON / OFF
- ピン留めしたアクセスポイント（BSSID 一覧）
- 選択した表示言語（Auto / 日本語 / English）

> **設定ファイルの保存先**:  
> `%LocalAppData%\WiFiAnalyzer\settings.json`  
> （例: `C:\Users\<ユーザー名>\AppData\Local\WiFiAnalyzer\settings.json`）  
> 日本語文字を含む設定は UTF-8 可視テキスト形式で保存されます。

---

## 6. インストール・アンインストール方法

### インストール方法
1. お使いのパソコンのアーキテクチャ（CPU）に応じたインストーラーを実行します:
   - 一般的な Intel / AMD 製 CPU（64ビット）: `WiFiAnalyzer_Setup_v<バージョン>_x64.exe`
   - Snapdragon 等の ARM 搭載 PC（Surface Pro 等）: `WiFiAnalyzer_Setup_v<バージョン>_arm64.exe`
2. セットアップウィザードが表示されます。画面の指示に従ってインストールを進めてください。
3. デスクトップ上にアイコンを作成するオプション（任意）を選択できます。
4. インストールが完了すると、スタートメニューまたはデスクトップショートカットから起動できます。

### アンインストール方法
1. Windows の [設定] → [アプリ] → [インストールされているアプリ] を開きます。
2. 一覧から「**WiFiAnalyzer**」を見つけ、[アンインストール] を選択します。
3. （または、スタートメニューの「WiFiAnalyzer」フォルダ内にある「WiFiAnalyzer のアンインストール」を実行します）

---

## 7. よくある質問・トラブルシューティング (FAQ)

### Q1. 周囲の Wi-Fi が 1 件も表示されず、「Wi-Fi アダプターが見つかりません」と出ます。
- **対処法**:
  - お使いの PC に Wi-Fi 機能（無線 LAN アダプター）が搭載されているか、無効（機内モードなど）になっていないかご確認ください。
  - USB Wi-Fi 子機をお使いの場合は、正しくポートに接続されドライバーが認識されているかご確認ください。

### Q2. すべての SSID が `<非公開ネットワーク>` と表示され、ネットワーク名が見えません。
- **原因**:
  - Windows 10 / 11 では、周囲の Wi-Fi SSID を取得するために **「位置情報サービス」の許可** が必要となる場合があります（SSID から位置が特定できるため OS のプライバシー保護機能が働きます）。
- **対処法**:
  1. Windows の [設定] → [プライバシーとセキュリティ] → [位置情報] を開きます。
  2. 「位置情報サービス」を **オン** にしてください。
  3. アプリを再起動またはスキャンを実行してください。

### Q3. メッシュ Wi-Fi の子機（Agent）のアクセスポイントが一覧に出てきません。
- **原因と対処法**:
  1. **電波強度が微弱な場合**: PC の Wi-Fi アンテナが子機の電波を受信できていない可能性があります。PC を子機に近づけて「🔄（更新）」を押してみてください。
  2. **ルーター側のステアリング制御**: 親機の電波が強い場合、ルーター製品の機能（メッシュステアリング等）により子機側が探索パケット（プローブ応答）を意図的に抑制することがあります。
  3. **非公開扱いになっている場合**: 子機の設定により SSID が空で通知されている場合、一覧の一番下（最下部）に *`<非公開ネットワーク>`* として表示されていることがあります。リストを最下部までスクロールしてご確認ください。
  4. **周波数帯フィルター**: 上部ツールバーのバンドフィルターが「すべて」になっているかご確認ください。

---
